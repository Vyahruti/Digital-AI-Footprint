services:
  - type: web
    name: digital-footprint-backend
    runtime: python
    pythonVersion: 3.11
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
      - key: DEBUG
        value: false
